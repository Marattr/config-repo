app:
  greeting: "Merhaba Config Server'dan!GTI REPO"

server:
  port: 8081

spring:
  security:
    jwt:
      secret: ${JWT_SECRET:I2locy5jb20udHIhaWhzY29tdHItc2VjcmV0LWtleSM=}
      #secret #ihs.com.tr!ihscomtr-secret-key#
  datasource:
    url: jdbc:postgresql://localhost:5432/murat
    username: murat
    password: ${DB_PASSWORD:murat}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      max-lifetime: 1800000
      keepalive-time: 60000
