app:
  greeting: "Merhaba Config Server'dan!GIT"

server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://${DATABASE_URL:localhost:5432}/customer
    username: murat
    password: ${DB_PASSWORD:murat}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      max-lifetime: 1800000
      keepalive-time: 60000
